[workspace]
resolver = "2"
members = ["core", "cli", "server"]

# ponytail: single-crate workspace today; clients/, server/, bindings/ join as
# they are built (ROADMAP S4/S5). The monorepo shape is decided, so the
# workspace is real structure, not speculative scaffolding.
