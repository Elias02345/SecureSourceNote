[package]
name = "ssn-core"
version = "0.0.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "SecureSourceNote shared core: object model, operation envelopes, durable local store, deterministic replay."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
